#include <stdio.h>
/*
int main(){
	int sco;
	scanf("%d", &sco);
	if(sco>=90) printf("수");
	else if(sco>=80) printf("우");
	else if(sco>=70) printf("미");
	else if(sco>=60) printf("양");
	else printf("가");
	return 0; 
}
*/

/*
int main(){
	int a, b, c;
	scanf("%d %d %d", &a, &b, &c);
	printf("%d", (b<=a && c<=a)? a:(a<=b && c<=b)? b:c);
	return 0;
}
*/

/*
int main(){
	char a;
	scanf("%c", &a);
	switch(a){
		case 'a' : printf("모음"); break;
		case 'e' : printf("모음"); break ;
		case 'i' : printf("모음"); break;
		case 'o' : printf("모음"); break;
		case 'u' : printf("모음"); break;
		default : printf("자음");
	}
	return 0;
}
*/

