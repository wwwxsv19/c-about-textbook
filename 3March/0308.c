#include <stdio.h> //stdio : STanDard Input Output. 표준 입출력. 기본적인 입출력 관련 라이브러리 함수들의 헤더 파일. 
//전처리 구문 : main 함수를 기준으로 main 함수보다 먼저 작성되는 문장들. 

main() //무조건 있어야 함. 
{
	/* 1. C언어의 시작 
	printf("Hello World!");
	getchar(); //키보드에서 문자 딱 하나를 입력받는 함수. 
	return 0;
	*/
	
	/* 2-1. C언어의 기초 
	printf("39 * 75 - 63 = 2787\n");
	
	int a=38, b=75, c=63;
	printf("%d * %d - %d = %d\n", a, b, c, a*b-c);
	
	a=a*b-c;
	printf("메모리[%d] = %d\n", &a, a);
	
	return 0; 
	*/
	
	/* 2-2. C언어의 기초 
	int a=2;
	float f=3.14;
	printf("%d %.1f", a*f, a*f); 
	*/
}

